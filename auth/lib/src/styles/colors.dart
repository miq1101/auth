import 'dart:ui';

///Creates all the colors which are used in app.

class AppColors {
  AppColors();
  Color primaryColor = Color(0xFF16BFBF);
  Color white = Color(0xFFFFFFFF);
  Color black = Color(0xFF000000);
}
